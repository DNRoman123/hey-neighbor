workflows:
  ios-appstore:
    name: Hey Neighbor iOS - App Store
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: HeyNeighborASC
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: app.lovable.heyneigh
      vars:
        BUNDLE_ID: "app.lovable.heyneigh"
        XCODE_PROJECT: "ios/App/App.xcodeproj"
        XCODE_SCHEME: "App"
        EXPORT_OPTIONS_PLIST: "/Users/builder/export_options.plist"
        APP_STORE_APPLE_ID: 19533586859
      node: 22
      xcode: latest
      cocoapods: default

    scripts:
      - name: Install dependencies
        script: npm install --no-audit --no-fund
      - name: Build web bundle
        script: npm run build
      - name: Sync Capacitor iOS project
        script: npx cap sync ios
      - name: Configure native Google Sign-In
        # Google client ids are public identifiers. A native release must have
        # the iOS id baked into Info.plist, so fail before archiving if the live
        # configuration is unavailable or malformed.
        script: |
          set -e
          CLIENT_ID="${GOOGLE_IOS_CLIENT_ID:-}"
          if [ -z "$CLIENT_ID" ]; then
            CLIENT_ID="$(curl --fail --silent --show-error --retry 3 \
              https://hey-neighbor-io.lovable.app/api/public/google-config \
              | sed -n 's/.*"iosClientId":"\([^"]*\)".*/\1/p')"
          fi
          case "$CLIENT_ID" in
            *.apps.googleusercontent.com)
              PLIST="ios/App/App/Info.plist"
              REVERSED="com.googleusercontent.apps.$(echo "$CLIENT_ID" | awk -F'.apps.googleusercontent.com' '{print $1}')"
              /usr/libexec/PlistBuddy -c "Delete :GIDClientID" "$PLIST" 2>/dev/null || true
              /usr/libexec/PlistBuddy -c "Add :GIDClientID string $CLIENT_ID" "$PLIST"
              /usr/libexec/PlistBuddy -c "Delete :CFBundleURLTypes" "$PLIST" 2>/dev/null || true
              /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes array" "$PLIST"
              /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0 dict" "$PLIST"
              /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0:CFBundleURLSchemes array" "$PLIST"
              /usr/libexec/PlistBuddy -c "Add :CFBundleURLTypes:0:CFBundleURLSchemes:0 string $REVERSED" "$PLIST"
              echo "Configured native Google Sign-In URL scheme."
              ;;
            *)
              echo "A valid iOS Google client id could not be resolved."
              exit 1
              ;;
          esac
      - name: Install CocoaPods (if the project uses them)
        script: |
          if [ -f ios/App/Podfile ]; then
            cd ios/App && pod install
          fi
      - name: Set build number
        script: |
          cd ios/App
          agvtool new-version -all "$(date -u +%y%m%d%H%M)"
      - name: Set up signing
        script: |
          set -e
          # The ios_signing environment block fetches and installs the matching
          # App Store profile and uploaded certificate before scripts run.
          xcode-project use-profiles \
            --project "$XCODE_PROJECT" \
            --archive-method app-store \
            --export-options-plist "$EXPORT_OPTIONS_PLIST" \
            --code-signing-setup-verbose-logging
      - name: Build .ipa
        script: |
          set -e
          xcode-project build-ipa \
            --project "$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME" \
            --config Release \
            --export-options-plist "$EXPORT_OPTIONS_PLIST"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
      email:
        recipients:
          - davidnroman@hotmail.com
        notify:
          success: true
          failure: true
